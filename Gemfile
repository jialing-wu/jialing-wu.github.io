source "https://rubygems.org"

# Jekyll version used by GitHub Pages. Pin for reproducible builds (local & CI).
gem "jekyll", "~> 4.3"

# Useful plugins (optional — add if you use them)
group :jekyll_plugins do
  # gem "jekyll-feed"
  # gem "jekyll-sitemap"
end

# Windows / JRuby compatibility
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]
